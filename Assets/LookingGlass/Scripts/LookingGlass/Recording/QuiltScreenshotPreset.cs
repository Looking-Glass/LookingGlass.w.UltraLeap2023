using System;

namespace LookingGlass {
    [Serializable]
    public enum QuiltScreenshotPreset {
        Custom = -2,
        Automatic = -1,
        Portrait,
        LookingGlass16,
        LookingGlass32,
        LookingGlass65
    }
}
